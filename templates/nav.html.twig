<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="#">Sortir.com</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('ville_list') }}">Villes
                    <span class="sr-only">(current)</span>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('campus_list') }}">Campus</a>
            </li>
            {% endif %}
            <li class="nav-item">
                <a class="nav-link" href={{ path('espaceAbonnes') }}>Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href={{ path('profil') }}>Mon Profil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href={{ path('logout') }}>Se déconnecter</a>
            </li>
        </ul>
    </div>
</nav>
